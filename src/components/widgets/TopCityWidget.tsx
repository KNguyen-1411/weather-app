import { CardHeader, CardTitle } from '../UI/card';

export default function TopCityWidget() {
    return (
        <>
            <CardHeader>
                <CardTitle>Thành phố hàng đầu</CardTitle>
            </CardHeader>
        </>
    );
}
